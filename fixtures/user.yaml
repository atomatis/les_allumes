App\Entity\User:
  user_admin:
    email: admin@email.com
    password: <generatePassword('mdp')>
    roles: ['ROLE_ADMIN']
    active: true
  user_base:
    email: user@email.com
    password: <generatePassword('mdp')>
    roles: ['ROLE_USER']
    active: true
